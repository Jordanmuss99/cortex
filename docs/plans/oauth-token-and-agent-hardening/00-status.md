# Status: OAuth Revocation and Agent Isolation

- Gate 1 — Product: APPROVED 2026-08-28
- Gate 2 — Architecture: APPROVED 2026-08-28
- Gate 3 — Program Design: APPROVED 2026-08-28 (revised secret boundary and staged executable)
- Gate 4 — Slice plan: APPROVED 2026-08-28

## Slices

- [x] Slice 1 -- tracer bullet: one fake bound MCP call projects/injects its agent and live revoke blocks the next call. COMPLETED 2026-08-28
- [x] Slice 2 -- disposable migration, bootstrap, database roles, and real readiness. COMPLETED 2026-08-28
- [x] Slice 3 -- DB-backed DCR, authorization-code, bound-token, and live-access happy path. COMPLETED 2026-08-28
- [x] Slice 4 -- immutable legacy JSON client/code import. COMPLETED 2026-08-28
- [x] Slice 5 -- restart-safe IP-bound browser login sessions and v1-cookie upgrade. COMPLETED 2026-08-28
- [x] Slice 6 -- transactional refresh rotation and durable identical retry. COMPLETED 2026-08-29
- [x] Slice 7 -- replay classification and whole-family revocation. COMPLETED 2026-08-29
- [x] Slice 8 -- RFC 7009 and local operator revocation/security controls. COMPLETED 2026-08-29
- [x] Slice 9 -- bounded legacy access and refresh-token upgrade bridge. COMPLETED 2026-08-29
- [x] Slice 10 -- hosted MCP agent isolation and all 29 ChatGPT schema projections. COMPLETED 2026-08-29
- [x] Slice 11 -- hosted REST allowlist, scoped status, and backend ownership closure. COMPLETED 2026-08-29
- [x] Slice 12 -- lifecycle readiness, pruning, fatal startup, and audit/log redaction. COMPLETED 2026-08-29
- [ ] Slice 13 -- private-network release candidate and demonstrated rollback paths.
- [ ] Slice 14 -- reversible production backup and additive preparation.
- [ ] Slice 15 -- live DB-authority cutover and real ChatGPT v2 upgrade/control proof.
- [ ] Slice 16 -- legacy closure, recovery drill, and scheduled security verification.

## Notes for a fresh session

- The initiative covers the two hardening items left after the Cortex ChatGPT plugin audit: immediate revocation/replay protection and binding each authorized identity to the Cortex agent it may access.
- Preserve the current low-friction experience: an active legitimate ChatGPT connection should continue refreshing without repeated sign-ins.
- A deliberate disconnect or security revocation must take effect promptly.
- Existing single-user Cortex connections need a safe compatibility and migration path.
- Gates 1-4 were approved on 2026-08-28. Gate 3 was reopened and reapproved before Slice 3 implementation with its explicit secret boundary and staged executable; Slices 1-2 remain complete and Gate 4's unchanged slice order remains approved.
- PostgreSQL is proposed as the sole OAuth authority; the current JSON state file is migration/rollback input only. Code consumption, grant creation, refresh rotation/replay decisions, revocation, and audit writes are transactional.
- Each authorization grant is bound server-side to one active principal-agent binding. Hosted ChatGPT schemas hide `agent_id`; the gateway injects it, accepts only equal cached values, and rejects mismatches before forwarding. Hosted REST uses a fail-closed route policy; `/agents` and unknown routes are blocked.
- Access and refresh JWTs gain a live grant `sid` and bound agent claim. Every protected request checks the active grant/principal/binding in PostgreSQL so revocation is immediate; database outages return a temporary service error and fail closed.
- Refresh rotation uses a configurable initial 30-second idempotent retry window for an identical just-consumed token, then revokes the whole family on reuse. This is the explicit reliability-versus-replay tradeoff for approval.
- Add RFC 7009 `POST /revoke`, a local operator revoke command, stateful IP-bound browser login sessions, security audit events, gateway readiness, and a least-privilege gateway DB role.
- Preserve the live ChatGPT connection by importing its DCR client and upgrading its first legacy refresh during a bounded rollout window. Legacy access gets no more than its old one-hour lifetime. The rollout must exercise the real connector before closing compatibility.
- Production raw REST/MCP is a privileged service/admin boundary and must move behind a private container network; an explicit development/admin override can retain loopback ports.
- OpenAI does not publicly promise that ChatGPT's Disconnect button calls `/revoke`. Test it during rollout; if it does not, reopen Gate 1 before claiming that UI action is server-authoritative. Cortex-side/operator revocation remains guaranteed.
- Gate 3 specifies the exact file set, public interfaces, database records/constraints, hosted route catalog, call stacks, and named tests. It uses one additive checked-in OAuth migration (`009_oauth_authority.sql`) after the existing idempotent Build 1-8 routine instead of expanding this patch into a full historical schema conversion.
- A one-shot migration owner applies DDL, provisions fixed least-privilege gateway/operator login roles, and bootstraps the explicit subject-agent binding. Runtime services only assert the migration checksum; the gateway imports the frozen JSON state under a data-migration lock and never writes it.
- The hosted policy covers exactly 15 REST route patterns and the 29-manifest MCP surface. Original JSON bytes are checked for duplicate agent selectors, accepted requests are canonicalized and reserialized, and all unlisted public routes/tools fail closed.
- Real integration tests use disposable PostgreSQL and include concurrent code exchange/refresh/revoke races, durable retry reconstruction, legacy migration, role-denial checks, full schema projection, two-agent ownership, network topology, rollback, and a staged live ChatGPT exercise.
- Gate 4 is approved in `04-slices.md` as 16 ordered vertical checkpoints. Slices 1-12 are development/staging only; Slice 13 freezes the release candidate; production preparation, cutover, and timed legacy closure are separate Slices 14-16.
- Migration 009's checksum is frozen only after the disposable rollback-qualified release candidate passes. Production is not mutated before that point. After the first v2 token is issued, the current stateless image is permanently disallowed as a rollback target.
- The Gate 1 metric requires scheduled end-to-end checks. Slice 16 therefore finishes only after one redacted verifier run succeeds from an approved trusted operator host; no production scheduler or credential store is assumed silently.
- Slice 1 is complete. It added a dormant dependency-injected tracer app, a final-shaped one-tool agent policy, immutable hosted-schema projection, and a real loopback HTTP proof. The fake store is queried for every request; after it returns `revoked`, the next identical call is `401` and total upstream requests do not increase.
- Slice 1 intentionally proves only the enforcement seam, not a database revoke transaction, persistence, refresh-family invalidation, alias/duplicate selector rejection, or production wiring. Those remain in Slices 2, 7, 8, and 10. Existing `server.js` is not wired to `app.js`; the container still starts `node server.js`.
- Slice 1 verification: OAuth gateway 34/34 tests passed; root Cortex 77/77 tests passed; syntax checks passed; the gateway image built; dormant modules import without side effects; tests are absent from the image; and the in-image legacy `server.js` SHA-256 remained `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`.
- Slice 2 is complete. It adds exact migration-file preflight before mutation, locked and checksummed migration execution, migration 009's OAuth authority schema, explicit subject-agent bootstrap, fixed least-privilege gateway/operator roles, and fatal Core/MCP/gateway readiness checks. Tests cover hostile search paths, URL/TLS policy, deferred grant supersession, lifecycle invalidation, and role-denial boundaries.
- Slice 2 verification passed on the final source: root 77/77, gateway unit 39/39, root PostgreSQL/security 16/16, and gateway/PostgreSQL 1/1. Both disposable PostgreSQL clusters and their network were removed after the run.
- Packaged verification passed with root image `sha256:49917cf8ec9ce199af9e42ae0997652697765c0cfdc2f90d10721f51fb8d5cdb`, gateway image `sha256:e68f135b60427d9c11d63cc787defc10f3475ba2119884dafe285545acd6c32d`, and migration SHA-256 `8f673ea2e5a4b4d8a23dc872d8cdf95ffa5acf1c7d86a3e74b1030d9d8190415`. Compiled preparation ran twice; packaged Core, MCP, and gateway readiness succeeded; missing-schema startup failed closed; runtime images contain no project tests.
- Independent OAuth/security, runtime-artifact, and final code reviews found no concrete Slice 2 blocker. No production database, deployment, or live connector state was changed. Migration 009 and floating dependency/image digests remain unfrozen until the Slice 13 release-candidate checkpoint.
- Slice 3 pre-implementation review found that `GatewayConfig` omitted the password/JWT-secret ingress required by its own dependency-injected app, and that immediately replacing the packaged v1 command would make a development checkout an accidental cutover mechanism. Gate 3 now specifies a separately validated, non-logged `GatewaySecrets` boundary and an explicit no-JSON `server-v2.js` command for disposable/staging proof; the packaged default remains v1 until Slice 13 promotes the database-aware path.
- The same Gate 3 correction makes refresh-JTI reconstruction explicit, tightens complete v2 claim validation, and adds database constraints for canonical unique redirect arrays, public-only dynamic clients, and principal-binding consistency. No implementation code or live state was changed before this checkpoint.
- Slice 3 is complete. Strict ChatGPT DCR, credential authorization, keyed one-time code exchange, generation-zero bound access/refresh issuance, atomic grant supersession/audit, and live PostgreSQL access validation now run through the dormant DB-only `server-v2.js` staging path. The legacy packaged command remains `node server.js`; no JSON state authority or fallback is reachable from v2.
- Slice 3 closed the review findings before completion: existing tokens survive TTL-default changes; expected credential digests are precomputed; registration/login limits use bounded keyed IP buckets and trust forwarded addresses only from configured proxy CIDRs; changed-schema startup rejects before binding; and the fixed compatibility audience set is coherent when `baseUrl` and `issuerUrl` differ.
- Final disposable verification passed 149/149 tests: root 77/77, gateway unit 50/50, PostgreSQL/security 16/16, and gateway/store integration 6/6. Coverage includes serialized DCR capacity, malformed callbacks, rollback-safe and concurrent one-time exchange, digest-only persistence, distinct issuer/base audiences, REST/MCP binding across restart, spoof-resistant rate limiting, and database-outage 503 behavior with no false OAuth challenge or upstream call. Disposable containers, volumes, and networks were removed.
- Final packaged gateway proof is `sha256:257782604e64b0d917da84a3f37d48c902ee64a89ad1baa952890684fadbfcbe`. Its default v1 entry SHA-256 remains `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`; its staged modules match source; tests/backups are absent; and default migration lookup returns SHA-256 `89c937eb7623bb506727e1245495657b150a133f6c96e040c18f6a06943e30fd`. Migration 009 remains mutable until Slice 13.
- Independent crypto/config, app/runtime, database/store, and packaged-artifact reviews found no remaining Slice 3 blocker. No production database, deployment, JSON state, credential, or live ChatGPT connector was read or changed. The next approved checkpoint is Slice 4; wait for the user's continue-or-re-steer decision.
- Slice 4 is complete. It adds a strict, bounded v1 state reader with duplicate-member and deep-nesting rejection, fatal UTF-8/shape/callback/scope validation, exact-byte SHA-256, no-follow mode-0600 regular-file reads, before/after metadata checks, and no source mutation. PostgreSQL receives only mapped client descriptors and purpose-keyed authorization-code digests; raw codes and file bytes cannot cross the store boundary.
- The one-time import serializes with dynamic registration, enforces the shared 1,000-client ceiling, resolves the configured active principal/default binding inside the transaction, skips but accounts for expired codes, writes a redacted audit event, and commits its checksum/count report marker last. Exact retry is idempotent; changed checksums, descriptor counts, clients, codes, bootstrap state, capacity, malformed markers, and persistence failures all fail atomically. `server-v2.js` receives only the expected checksum and parsed client/code counts, never reads or writes JSON, and refuses readiness until the exact imported marker is present.
- The production-shaped proof ran the actual v1 `server.js`, registered a ChatGPT DCR client, created one live outstanding PKCE code with noncanonical scope order, froze the resulting file, mapped every parsed record, canonicalized scopes, imported it, started v2, exchanged the imported code, rejected an altered-byte checksum, and returned the same report on restart. It also confirmed unchanged source bytes/mode and scanned all nine OAuth authority tables plus gateway logs without finding raw codes, bearer tokens, browser credentials, or signing secrets.
- Final scoped verification passed: 61/61 gateway unit tests, 6/6 focused disposable store tests, and 8/8 disposable gateway integration tests. Independent parser/filesystem, store/database, and runtime/integration security reviews found no remaining Slice 4 blocker. Syntax checks and scoped diff checks passed.
- Packaged runtime proof passed with image `sha256:2afcee843b3370caf245c4b23053dafc359ae988c884bcbcf51b58a0c19dad5a`. Its default command remains `node server.js`; staged v2 was launched only explicitly. Packaged v1 produced a 615-byte mode-0600 snapshot with one DCR client and one live code, SHA-256 `6e9f008118541f479e2e899215d7969c0503fedaced05022f88521e4d6ee6fa5`. Read-only first import and separate-process retry returned `imported` then `already_imported`; explicit v2 returned ready and exchanged the code. Final checksum, size, and mode were identical, all secret-canary log checks passed, runtime files matched reviewed source, tests/backups were absent, and every disposable resource was removed.
- Cutover precondition: the frozen v1 file must contain only canonical ChatGPT DCR clients and callbacks accepted by the strict reader, with trimmed client names and no raw query/fragment delimiters. Static Linear/Notion outstanding-code handling remains deferred until Slice 13 static-client synchronization. A fresh-install marker/checksum outcome is not yet approved, so missing import state deliberately remains fatal rather than being guessed.
- The broader orchestration blocker recorded at the Slice 4 boundary was resolved by concurrent workspace work before Slice 5's final verification. Slice 5 preserved that separate Core/migration work; the current `npm run test:oauth:integration` workflow is now fully green as recorded below.
- No production database, deployment, state file, credential, connector registration, or live ChatGPT connection was read or changed. The next approved checkpoint is Slice 5; wait for the user's continue-or-re-steer decision.
- Slice 5 is complete. Browser authorization now uses a random opaque cookie whose purpose-keyed digest is stored in PostgreSQL with the configured principal epoch, a keyed canonical client-IP fingerprint, fixed issue/expiry timestamps, and optional revocation. Resolution rechecks the live active principal and exact active default binding on every use; changing or omitting the cookie, changing the trusted IP, expiry, revocation, principal disablement/epoch change, or binding revocation/non-default status returns to credentials. Grant revocation deliberately leaves the browser session usable.
- The unchanged v1 signed-cookie format has a strict, bounded compatibility verifier using the original raw JWT-secret bytes and exact v1 HMAC framing. One verified v1 cookie can create exactly one opaque replacement under the existing partial unique index, concurrent losers do not erase the winner, and the replacement preserves rather than extends the verified v1 expiry. Raw cookies, client IPs, credentials, code material, and database error details do not cross the store/audit boundary.
- The authorization HTTP path accepts exactly one bounded `__Host-cortex_oauth_session` cookie and emits `Secure; HttpOnly; SameSite=Lax; Path=/` with no `Domain`. Trusted ingress peers without one canonical client-IP header fail closed with 503 and no cookie mutation; untrusted/direct peers ignore forwarding headers. A database outage never clears a still-valid cookie. If a session or v1 upgrade commits immediately before authorization-code storage becomes unavailable, the 503 now carries the hardened committed cookie so retry remains password-free.
- Review closed two transaction-boundary defects before completion. Every session create/resolve/upgrade returns the exact frozen `BoundPrincipal`, and authorization creation requires that exact principal, epoch, binding/version, agent, and scope context. A code-table transaction barrier orders code creation against security logout/issuer sweeps: a code is either committed before and consumed by logout or rejected after it; a deterministic queued-lock proof leaves no post-logout code or audit row. Store failures discard PostgreSQL causes, with recursive canary checks proving keyed digests and fingerprints are absent from exposed errors.
- The production-shaped proof ran the actual unchanged v1 `server.js`, captured its real signed browser cookie, raced two v2 upgrades, observed exactly one 302 winner and one safe form loser, confirmed the database expiry exactly matched the source cookie, rejected a mutated cookie, scanned session/audit rows for both raw cookie values, restarted the gateway, and reused the winner's opaque cookie from the same IP without credentials. Store proofs separately cover IP mismatch, missing/changed cookie, expiry, explicit session revocation, epoch change, disabled principal, revoked/non-default binding, grant independence, audit rollback, and restart durability.
- Final current-tree verification passed 190/190 checks: root Cortex 93/93, gateway unit 70/70, root PostgreSQL/security 16/16, and gateway/database integration 11/11. Focused Slice 5 database security proofs passed 3/3, the crypto compatibility suite passed 16/16, syntax and scoped whitespace checks passed, and every disposable database container, volume, and network was removed. Concurrent workspace work extended readiness to the new memory migration 010; it was preserved and the final proof used the current exact 009+010 ledger rather than reverting that separate work.
- Packaged gateway proof is `sha256:a058db58f31dfd3591d8efc2887d882fae4d08eb35863b514089bea0c3749d54`. Its default command remains `node server.js`, the unchanged v1 entry hash remains `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`, reviewed staged modules match the image byte-for-byte and import without side effects, and tests/backups are absent. Migration hashes in the image are 009 `89c937eb7623bb506727e1245495657b150a133f6c96e040c18f6a06943e30fd` and concurrent 010 `65b5c50dee47697371b6d246117072bac958079e231a1d6869eda96bb52df31e`.
- Independent crypto/compatibility, app/runtime, and store/database security reviews found no remaining Slice 5 blocker after the review-driven fixes. No production database, deployment, state file, credential, connector registration, or live ChatGPT connection was read or changed. The next approved checkpoint is Slice 6; wait for the user's continue-or-re-steer decision.
- Slice 6 is complete. The v2 token endpoint now rotates the current refresh generation under a grant-first, generation-second lock order using one PostgreSQL wall-clock value captured after the family lock. Consumption, replacement insertion, grant advancement, the exact configurable retry deadline, and the audit event commit atomically; signing occurs only after that decision commits. Raw refresh tokens and JTIs never cross the persistence boundary: PostgreSQL receives purpose-keyed 32-byte digests, a keyed semantic request fingerprint, and a random reconstruction nonce.
- Two concurrent presentations of the same current generation now converge on one persisted replacement. An identical retry of the immediately previous generation reconstructs the bit-identical refresh JWT from that descriptor, including after gateway/store recreation, and ignores each loser's candidate material. Requested scope may narrow but never expand; the resulting access token is evaluated with only its effective scopes. Public-client authentication is rejected consistently before either token grant is dispatched, and storage outages remain `503 temporarily_unavailable` without an OAuth challenge.
- Refresh audit rows intentionally populate only `grant_id`: adding principal, binding, or client foreign keys after taking the grant lock would reverse the authorization/operator parent-first order and can deadlock. Gate 3 permits nullable audit references, and the grant provides transitive attribution. Slice 12 must retain every grant referenced by a retained audit event for at least the audit-retention lifetime, or introduce a separately reviewed non-locking attribution design, and must prove pruning cannot orphan refresh-event attribution.
- Replay-shaped cases are classified but do not revoke or otherwise mutate the family in Slice 6. Late, older-generation, changed-fingerprint, and unknown-JTI family revocation remains exactly at the approved Slice 7 boundary.
- Final current-tree verification passed 229/229 checks: root Cortex 132/132, gateway unit 70/70, root PostgreSQL/security 16/16, and gateway/database integration 11/11. Coverage includes injected-audit rollback, a held client-row deadlock regression, concurrent winner/retry convergence, exact 30-second persistence, restart reconstruction, scope narrowing, public-client rejection without code consumption, database-outage behavior, and database leak scans containing every refresh bearer and raw JTI canary. Syntax and scoped whitespace checks passed, and all disposable PostgreSQL containers, volumes, and networks were removed.
- Packaged gateway proof is `sha256:71d14f7cdec9108baf8daa7501da0485cd22b9f6b3b31740c442a72856f3732a`. Its default command remains `node server.js`; the unchanged v1 entry hash remains `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`. Source/image hashes match for `app.js` `8b4b3f68efc370cb34b8cdfa8890def0b5d58d6745535ba4977a65d0e9a71396`, `oauth-crypto.js` `e542ee57d26eee21429fca04867a84c85dbd561292cf9a3871bf23a88826eb37`, `oauth-store.js` `67d565228f23e70ba2b235185374d88fca59e2a746dbaa53217a20d384cb23a9`, `server-v2.js` `e5a4a1fbbee7d5ff0816355260e80f00f69f88919d34ec26f76aad072d0ed66a`, migration 009 `89c937eb7623bb506727e1245495657b150a133f6c96e040c18f6a06943e30fd`, and concurrent migration 010 `9ecdffd220283bb474b32a1d55f6c4c8230b177e745e2f93e3cb80d6cd4b1942`. Dormant v2 modules import without side effects; tests, backups, and editor residue are absent from the image; the temporary image tag was removed.
- Independent HTTP/crypto, store/database, plan-conformance, final adversarial, and packaged-artifact reviews found no remaining Slice 6 blocker. No production database, deployment, state file, credential, connector registration, or live ChatGPT connection was read or changed. The next approved checkpoint is Slice 7; wait for the user's continue-or-re-steer decision.
- Slice 7 is complete. Late previous-generation reuse, generations older than the one retryable parent, a changed retry fingerprint, and an unknown signed JTI for a known `sid` now commit one whole-family revocation. The grant-first transaction captures one PostgreSQL wall-clock value after obtaining the family lock, marks the active grant `revoked` with the fixed non-secret reason `refresh_replay_detected`, constraint-safely clips every still-live refresh descriptor, and appends one grant-only `refresh_replay` audit before returning. Any mutation or audit failure rolls the entire decision back and exposes only the generic persistence error.
- The adversarial database proof injects replay-audit failure and confirms the grant, generation, refresh expiries, access decision, and audit history all roll back. It covers late, older-generation, changed-fingerprint, unknown-JTI, and simultaneous replay; forces both rotation-first and replay-revocation-first lock orderings with database barriers; proves a just-returned refresh/access pair cannot resurrect the family; and leaves a family created before every attack active and rotatable. Repeated presentations after the winning replay observe the revoked grant and cannot create a second replay audit.
- The HTTP proof constructs a validly signed same-generation refresh token from a fresh unpersisted reconstruction nonce, receives `400 invalid_grant` with `Cache-Control: no-store` and no challenge, and observes the associated access token fail on its next request. A separate oldest-generation replay revokes a three-generation family; the next REST and MCP calls both return the exact `401 invalid_token` challenge without reaching either upstream. Database assertions verify the fixed grant reason, clipped family, one redacted grant-only audit with the internal classification, and absence of every bearer/JTI canary.
- Final current-tree verification passed 246/246 checks: root Cortex 148/148, gateway unit 70/70, root PostgreSQL/security 16/16, and gateway/database integration 12/12. Syntax and scoped whitespace checks passed, and both disposable PostgreSQL clusters, volumes, and networks were removed. Independent implementation, contract, final adversarial, and packaged-artifact reviews found no Slice 7 blocker.
- Packaged gateway proof is `sha256:cb38477dd6f7a275957c9cf7b15343a8c3ffa38b850b1a329b377b12ee75b797`. Its default command remains exactly `node server.js`; source/image hashes match for `server.js` `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`, `app.js` `8b4b3f68efc370cb34b8cdfa8890def0b5d58d6745535ba4977a65d0e9a71396`, `oauth-crypto.js` `e542ee57d26eee21429fca04867a84c85dbd561292cf9a3871bf23a88826eb37`, `oauth-store.js` `a25523bd8ae51b8faf33dd6c87547da5f755c21cc3b69bf2260d8c9c59505a43`, `server-v2.js` `e5a4a1fbbee7d5ff0816355260e80f00f69f88919d34ec26f76aad072d0ed66a`, migration 009 `89c937eb7623bb506727e1245495657b150a133f6c96e040c18f6a06943e30fd`, and concurrent migration 010 `e746097ad980a4499465d1257a7cda3d2c5169dcfb11c807b622dadff640a2ae`. Dormant v2 imports have no startup side effects under a read-only, network-disabled container; tests, backups, editor residue, VCS metadata, and coverage files are absent; temporary resources were removed.
- Slice 7 intentionally does not add RFC 7009 `/revoke`, revocation discovery, token-reference revocation, operator controls, or the explicit operator/client-revoke race; those remain Slice 8 work. Grant-only replay-audit attribution still requires the approved Slice 12 retention rule. No production database, deployment, state file, credential, plugin registration, or live ChatGPT connection was read or changed. The next approved checkpoint is Slice 8; wait for the user's continue-or-re-steer decision.
- Slice 8 is complete. Both discovery documents now advertise the RFC 7009 endpoint and public-client authentication method. `POST /revoke` accepts only form input, identifies one active public client, verifies current or expired Cortex v2 access/refresh references without weakening signature, issuer, audience, type, binding, lifetime, or claim checks, and revokes the referenced grant family. Malformed, unknown, repeated, cross-client, hint-mismatched, and terminal-family inputs converge on the same empty `200` after valid client identification; invalid client authentication remains distinct, parser failures are generic/no-store, and database failure fails closed. Revocation is independent of the browser cookie and leaves that login session reusable.
- The client path rechecks the signed client, authenticated client, persisted grant client, client status, and public authentication method inside a grant-first transaction. One post-lock database timestamp drives the grant transition, constraint-safe refresh clipping, and grant-only audit. Tests cover database-client mismatch, client deactivation, injected audit rollback, exact repeat idempotency without audit spam, unrelated-family isolation, rotation-first and revoke-first races, immediate REST/MCP rejection without upstream work, browser-session preservation, and bearer/JTI absence from grant, refresh, and audit rows.
- The local operator surface now uses one strict fixed-role PostgreSQL URL, asserts the exact login/database/search path, reads only four redacted views, and can invoke only the six approved fixed-search-path SECURITY DEFINER functions. Revoke-one, revoke-all, principal disable, binding disable, security logout, and issuer invalidation serialize code/grant/session authority in a deterministic order, use post-lock database time, consume outstanding authorization codes where required, and audit atomically. The CLI has exact parsing and issuer confirmation, allowlisted outputs, stable exit codes, no cause propagation, and terminal-safe JSON/human formatting that escapes C1 and Unicode format controls. A disposable end-to-end proof exercised the real CLI, operator identity, redacted listing, real and no-match revocation, rollout status, and every remaining database-result mapping rather than mocking the adapter seam.
- Final current-tree verification passed 276/276 checks: root Cortex 159/159, gateway unit 84/84, PostgreSQL/security 19/19, and gateway/database integration 14/14. Focused parser/crypto/operator tests passed 49/49; repository-wide TypeScript, syntax, and scoped whitespace checks passed; and the disposable containers, volumes, and network were removed. Independent HTTP/crypto/store and operator/SQL reviews found no remaining blocker after resolving an oversized-body error classification, C1/bidirectional terminal escaping, and the missing real-adapter proof. Concurrent non-OAuth workspace work was preserved.
- Final packaged gateway proof is image `sha256:8cd50f314a7be52240cc083b5234dc28efc3380ef3c3b04b98ed5ba6141190bb` (platform manifest `sha256:4775decb04791eb7b1e2148e7ea4cdb2238d45c807379ffb1b481a750b2d1b5d`, config `sha256:7355ed83c2b50e9a192c948a57c48bdc6b9c1a5a292d9dbef5ac1b1bf9eca824`). Its default command remains exactly `node server.js`, and the unchanged v1 entry hash is `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`. Source/image hashes match for `admin.js` `b88b8f48bd026d4f772c801d294293ee6362738f3ff0b52ba031f94bea5bf55f`, `app.js` `065f80719417b2744ed70cc18568284d30e6e51637795f91f5a38dec53d9283d`, `oauth-crypto.js` `0cd7ad1d01cf182c48d0729eb67f0a56a0f6e01fda465e042918aa90dbc5d3cd`, `oauth-store.js` `67f999d6032ca13ccea71bd1a86317f9ed03817e17c8b88fd2cf2c0baa6d8f61`, `server-v2.js` `e5a4a1fbbee7d5ff0816355260e80f00f69f88919d34ec26f76aad072d0ed66a`, migration 009 `8b886d3cdbf267eec529d4d0c802d53d2bd75f10004663b1bf348177219869c8`, and concurrent migration 010 `97a82ae5964131d3ea732117694bcfd5cc2adb838d6ba9e3b3dead01928b5c33`. Dormant staged modules import with zero handles/requests under read-only, network-disabled execution; only standard image environment is present; tests/backups are absent; and invoking the packaged admin without a command exits before database access. Temporary image tags were removed.
- Slice 8 intentionally does not implement the bounded legacy-token bridge, complete 29-tool hosted schema isolation, hosted REST closure, lifecycle pruning, or deployment cutover. Migration 009 remains mutable until Slice 13, and concurrent migration 010 was preserved. No production database, deployment, state file, credential, plugin registration, or live ChatGPT connection was read or changed. The next approved checkpoint is Slice 9; wait for the user's continue-or-re-steer decision.
- Slice 9 is complete. The dormant database-authority gateway now strictly verifies the exact sid-less v1 access/refresh formats only before one immutable cutoff, requires the frozen imported-state marker and original imported ChatGPT DCR client, and rechecks the active bootstrap principal/default binding, token-effective scopes, expiry, future skew, import completion, and `legacy_not_before` against live PostgreSQL authority. Static, ambiguous, unimported, or post-cutoff legacy tokens fail closed, no code issues a new sid-less token, and v2 verification plus v2-only non-oracular `/revoke` behavior remain unchanged.
- The first valid legacy refresh atomically creates one bound grant, a consumed legacy generation `-1`, a reconstructable v2 generation `0`, and a redacted audit event. Concurrent first presentations converge; an exact semantic retry reconstructs the identical generation-zero bearer after restart. The permanent keyed tuple identity is exactly `(issuer, subject, client_id, resource)`, independent of scope and binding. Historical JTIs, changed semantic retries, late retries, and legacy reuse after v2 rotation revoke and clip only that migrated family atomically; permanent digest tombstones and prior/newer v2 grants survive. Raw legacy bearers, JTIs, tuple material, signing secrets, and database causes are neither persisted nor logged.
- Final adversarial review closed three boundary defects before completion: prior-family lookup now sees v2 grants on historical bindings; changed request client/resource reaches fingerprinted replay handling while first-use mismatches remain invalid; and cutoff parsing rejects rollover/noncanonical timestamps while retaining the original expired timestamp as a restart-safe permanently closed state. Tests cover active-v2 preservation across bindings, audit-failure rollback for first migration and changed/historical replay, strict cutoff and `legacy_not_before`, restart reconstruction, database outage behavior, legacy access on REST/MCP with agent injection, and secret-canary scans.
- Final Slice 9 verification passed 120/120 scoped checks: gateway unit 87/87, PostgreSQL/security 19/19, and gateway/database integration 14/14. Syntax and scoped whitespace checks passed, and every disposable PostgreSQL container, volume, and network was removed. The aggregate repository runner is presently blocked before its OAuth stages by separate untracked reflection work whose test imports exports that its companion script does not provide; 162 other root assertions passed in that attempt. Repository-wide TypeScript likewise reports only concurrent non-OAuth reflection, screen-observer, and MCP ingest type errors; those unrelated files were preserved rather than altered in Slice 9.
- Final packaged gateway proof is image `sha256:c185f090155f06bc629f7aa3c825e6e4e205e512dd7ac8a4bbe72e92fd1b9a18` (platform manifest `sha256:3d1905e4a5d40296f3f41f0a4916fe4a14d6e56075f652b33b9d823590d491fb`, config `sha256:6424a254bf882f5900ea223fbd10eb4af008bdfd418dabe5c24a9a8361ffd52e`). Its default command remains exactly `node server.js`. Source/image hashes match for `server.js` `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`, `config.js` `eba8177a2d60b32025859754fc3e259d468884b8731fab40da0950d6718f14b9`, `app.js` `74c1dded0cc0975fceb79e098568ee69903ab8e095666ef5019dcc8750636a8c`, `oauth-crypto.js` `86281d1b57b88a0143eba77049504447be412b8fec8cb5049b26254ce69ae1bb`, `oauth-store.js` `4a1f52943db6b9567613e7c5ac72e9388000e4cb58fb90d77a92afc1323c6ca4`, `server-v2.js` `57418f2af26c7ff6a094cd42a24f6e7293c846a540da5354119700ba1e8c313e`, migration 009 `940d0004ff7954f7725db58cfc803c4b46c1ae2713e1bf0570cfb0020a7a0dcb`, and preserved migration 010 `97a82ae5964131d3ea732117694bcfd5cc2adb838d6ba9e3b3dead01928b5c33`. Dormant modules import with zero handles/requests under read-only, network-disabled execution; tests, backups, editor residue, coverage, and VCS metadata are absent; the temporary image tag was removed.
- Independent crypto/HTTP, database/store, final contract, and packaged-artifact reviews found no remaining Slice 9 blocker. Slice 9 intentionally does not implement the complete 29-tool hosted schema/agent-isolation surface, hosted REST closure, pruning, or deployment cutover. Slice 12 must retain every non-null legacy tuple-digest grant as a permanent replay tombstone. Migration 009 remains mutable until Slice 13, and no production database, deployment, state file, credential, plugin registration, or live ChatGPT connection was read or changed. The next approved checkpoint is Slice 10; wait for the user's continue-or-re-steer decision.
- Slice 10 is complete. The dormant database-authority gateway now exposes exactly the 29 registered MCP tools and exactly five protocol methods (`initialize`, `notifications/initialized`, `ping`, `tools/list`, and `tools/call`). The hosted catalog is projected from every current production Zod input/output schema: only the top-level input `agent_id` selector is removed, output schemas remain intact, authoritative descriptions retain appended safety guidance, and the exact read/write scope and destructive/idempotent metadata are attached. Missing, partial, duplicate, extra, unknown, or malformed catalogs fail closed rather than silently shrinking ChatGPT's tool surface.
- Original request bytes are captured before JSON parsing and inspected with fatal UTF-8, 64-KiB, and depth-64 bounds. Duplicate or escaped canonical selectors, `agentId` aliases, selectors in the wrong envelope location, ambiguous envelopes, parser/raw disagreement, batches, and unknown methods/tools are rejected before upstream work; selector-looking keys nested inside tool content remain ordinary content. A missing selector is injected from the live bound principal, one equal cached selector is accepted, and every accepted request is cloned and serialized with exactly one canonical bound `agent_id`. Live authorization and the exact tool scope are checked before selector mismatch, so foreign-agent probes cannot become a scope oracle; `agent_mismatch` is a stable challenge-free `403`.
- Hosted `tools/list` projection is proven for JSON and SSE, including LF, CRLF, and CR framing. Non-2xx metadata responses, absent or unsupported media types, malformed JSON-RPC, and incomplete catalogs return a stable gateway failure. Non-list JSON/SSE tool calls remain incremental after canonical rewriting. A strict connector-header allowlist preserves only content negotiation and MCP session/protocol continuity; authorization, cookies, edge identity, API keys, and stale response framing are not forwarded. Downstream disconnects abort the upstream fetch.
- Final scoped verification passed 129/129 checks: gateway unit 96/96, PostgreSQL/security 19/19, and gateway/database integration 14/14. The current root suite passed 178 assertions and has one unrelated concurrent `reflection-ingest` failure; that out-of-scope work was preserved. Repository TypeScript, JavaScript syntax, scoped whitespace, actual-schema compilation, adversarial selector boundaries, full 29-tool scope enforcement, JSON/SSE streaming, and credential-scrubbing proofs passed. Both disposable PostgreSQL environments and all associated containers, volumes, and networks were removed. Independent schema, raw-policy, and transport/security reviews found no Slice 10 blocker.
- Final packaged gateway proof is OCI image index `sha256:b49bc5ca2308116499642ab23c1d07b8513fb744b5113ae55509b1d53af5fadb` (platform manifest `sha256:9bb93e1914fd39543676967eb705c4bb1162acc1064c68949ceb8272ba96a5a6`, config `sha256:74a6fbb5097ae7ed77b88b98f9ca80ec5873afe56f0f876d61a088909b8d1f4e`). Its default command remains exactly `node server.js`, and the unchanged v1 entry hash remains `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`. Source/image hashes match for `app.js` `fd21588b41c74d5c1a69a0c82d89ef96fa19526596c3795512f596a482c0937d`, `agent-policy.js` `e90153409389f6daec91f0bab876ef34ac585c39063994fec9f2b65cff84f1fe`, `chatgpt-tools.js` `1061acad70fb3f43f4e6f72aad2e8fac2b4280409e3bf3ac69d926e172180c9f`, migration 009 `940d0004ff7954f7725db58cfc803c4b46c1ae2713e1bf0570cfb0020a7a0dcb`, and preserved migration 010 `97a82ae5964131d3ea732117694bcfd5cc2adb838d6ba9e3b3dead01928b5c33`. Production Ajv schema compilation and all dormant modules work under read-only, network-disabled execution; tests, backups, editor residue, coverage, and VCS metadata are absent; the temporary review image and containers were removed.
- Slice 10 intentionally does not implement the hosted REST allowlist or backend ownership closure assigned to Slice 11, lifecycle pruning, release promotion, or deployment cutover. No production database, deployment, state file, credential, plugin registration, or live ChatGPT connection was read or changed. Migration 009 remains mutable until Slice 13. The next approved checkpoint is Slice 11; wait for the user's continue-or-re-steer decision.
- Slice 11 is complete. The dormant database-authority gateway now admits exactly the approved 15 hosted REST routes and the public agent-neutral health exception. It rejects `/agents`, unknown or method-mismatched routes, encoded separators/dots, malformed paths, and selector pollution without upstream work. Protected calls check live authorization and the exact read/write scope before agent mismatch, inspect the original bounded JSON bytes, accept only zero selectors or one equal canonical selector in the route-defined location, inject the bound agent, canonically reserialize the request, scrub credentials and edge headers in both directions, reject redirects, preserve response streaming, and abort upstream work when the caller disconnects.
- Every bound Cortex status statistic is now constrained to the selected agent, including memories, synapses with both endpoint memories, resonance, both breakdowns, last dream, and agent count; the raw internal aggregate remains available only when no selector is supplied. Procedural source-memory IDs are normalized to unique positive PostgreSQL `int4` values with a 100-unique cap, checked for ownership before embedding, and rechecked under a shared lock in the insertion transaction. Foreign and nonexistent sources are indistinguishable, while execute and refine retain combined record-and-agent predicates. REST and MCP adapters expose only stable, non-sensitive errors, and the projected ChatGPT schema carries the same bounds.
- Final current-tree verification passed: TypeScript typecheck; root Cortex 192/192; gateway 101/101; focused status/procedural isolation 9/9; disposable memory/database and real REST/MCP adapter proof 31/31; and the previously landed disposable OAuth security suites 19/19 plus 14/14. The live proof used two agents, exercised cross-agent source, execute, refine, status, deduplication, and least-privilege runtime-role boundaries, then removed every temporary container, volume, and network. Independent REST/transport and backend/database reviews found no remaining Slice 11 blocker.
- Packaged proofs passed with gateway image `sha256:214a99d5214893076d889bfb321a365e22f61e809bdab9ae46a911209d23afdd` and Core image `sha256:5a85757e05a886b0dc8c325aaa61d5348bc4723cc33344702e13edd2e6e7fd27`. The gateway default remains exactly `node server.js`; source/image hashes match for `server.js` `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`, `app.js` `b0cc04504a561fe434eebe7c9456118f9b0161dfb175b73edccd363358f04b84`, `agent-policy.js` `9295455892ca792c7155a6bd0e128ea165c51fb3dcf3326b85b22fcc7c121565`, `chatgpt-tools.js` `1061acad70fb3f43f4e6f72aad2e8fac2b4280409e3bf3ac69d926e172180c9f`, migration 009 `940d0004ff7954f7725db58cfc803c4b46c1ae2713e1bf0570cfb0020a7a0dcb`, and preserved migration 010 `97a82ae5964131d3ea732117694bcfd5cc2adb838d6ba9e3b3dead01928b5c33`. Both images import the reviewed modules read-only with networking disabled, Core fails closed without database configuration, and project tests, backups, VCS metadata, and coverage are absent.
- Slice 11 intentionally does not implement lifecycle pruning/readiness, release promotion, deployment, or live connector cutover. No production database, deployment, state file, credential, plugin registration, or live ChatGPT connection was read or changed. Migration 009 remains mutable until Slice 13. The next approved checkpoint is Slice 12; wait for the user's continue-or-re-steer decision.
- Slice 12 is complete. Migration 009 now provides fixed 93-day audit retention, permanent legacy-family replay tombstones, attribution-preserving grant retention, bounded `SKIP LOCKED` pruning, fixed-search-path lock/prune functions, and least-privilege runtime execution. The gateway runs initial and daily pruning through a dedicated timeout-bound pool, keeps it single-flight, and spends one fixed shutdown budget across HTTP drain, pruning, and all database pools. Strict event-specific allowlists now sanitize both local security logs and persisted security metadata; executable canaries cover bearer, token, code, cookie, password, JTI, raw-IP, and foreign-agent material.
- Gateway `/livez` is process-only. `/readyz` now requires exact database/schema/bootstrap/import authority, raw Core readiness, and a complete stateful MCP lifecycle (`initialize`, `notifications/initialized`, bounded `ping`, and confirmed `DELETE`). MCP handshakes and cleanup have independent deadlines, public probes coalesce and cache one result for five seconds, and cached failures cannot amplify child creation or logging. Core binds its probe surface before database work, keeps all application routes gated, coalesces readiness behind a two-second request deadline plus transaction-local one-second PostgreSQL timeout, and routes schema or listener failures through one redacted bounded shutdown. MCP installs signal/stdin EOF hooks before initialization and closes transport/database state within a fixed deadline.
- Final current-tree verification passed 370/370 checks: root Cortex 225/225, gateway unit 109/109, PostgreSQL/security 21/21, and gateway/database integration 15/15. TypeScript, the focused Core/MCP lifecycle suite (10/10), schema projection against the actual 29 Zod tools, retention/pruning concurrency, hostile-search-path role boundaries, stalled dependency/cleanup cases, and secret-canary scans passed. A packaged real-Supergateway proof completed the full MCP handshake under the default three-second gateway deadline and confirmed the temporary stdio child no longer existed after session deletion. Every disposable database, container, and network created by the Slice 12 work was removed.
- Packaged proofs passed with gateway OCI index `sha256:e76d4e27f90b22cb2b5a358170c54e05f6efc8a5400b61b78e06f786a2b1f4f7` (platform manifest `sha256:e3a22af0d88a6d0554b4acf0d68b232dffc8d168d6f2a7a7a6651b532fba8cf1`, config `sha256:9cafad8b2487e4969d3552eb56b71c11976b1e0d83611e05e96c1090b50789b2`) and Core index `sha256:651b0d0c441699467bf919ccd10d8ae3520dd5eb7f7db592ba02fcada4027bcf` (platform manifest `sha256:bc3f73c632af4563e224bb51ef57d8463fed9896aed9cad2a2cdb570babfe866`, config `sha256:c0f1424649149d81025c957252079edf8a83b39561b8d0e275ebcfd6e6552ea8`). The gateway default remains exactly `node server.js`, and the legacy entry hash remains `c75be0cc9f6c042ba4af2c79293bbf38b35168a02303212eea6b829235c4e831`. Source/image hashes match for `app.js` `6c2de69607626e77dbce8f255a6d7b145a20cf18362be29dd4ff7d0ca4ae31bc`, `oauth-store.js` `6be5a152a937bee5a5c5c7f02f990f1469a15799bdfc3c21cfa1f8d32e8ac696`, `security-log.js` `3596cc6ce45cb23fa81929f65b0b4de8c2a9dedc6b084b1807449ef232ce323c`, `server-v2.js` `18a264f4a03a1053d6f0ba2766de23ef16d8ef52c1e064bb566836b671925adf`, migration 009 `e0c2de152746cb4b9d3a91710dcf71149062dc53b7b55ff1608f4f8d9b629d80`, and preserved concurrent migration 010 `c41cdcbd0bdef50d673dbb19bbdea47403d4b8e8981c7d2438f6a1ead8789df5`. Reviewed modules import read-only with networking disabled, only standard image environment is present, project tests/backups are absent, and all temporary Slice 12 image tags were removed.
- Independent database/retention, pruning/shutdown, active-MCP, and Core/MCP lifecycle reviews found no remaining Slice 12 blocker after the review-driven fixes. Slices 1-12 remain development/staging only: no production database, deployment, OAuth state, credential, plugin registration, or live ChatGPT connection was read or changed. Migrations 009 and 010 remain mutable until the Slice 13 release-candidate boundary. The next approved checkpoint is Slice 13; wait for the user's continue-or-re-steer decision.
